Utilisation des codes (fichiers .ipynb) :

- Choisir les paramètres dans le premier bloc (epochs, val_subsplits, buffer_size, batch_size)

- Modifier les chemins des repertoires afin de récupérer les bonnes images

- Lancer l'execution des blocs à la suite